namespace Prompteer.Web;

// Marker class for shared localization resources.
// Used as the type parameter for IStringLocalizer<SharedResource>.
// NOTE: Must be in the assembly's root namespace (Prompteer.Web) so that
// ResourceManagerStringLocalizerFactory resolves Resources/SharedResource.resx correctly.
public class SharedResource;
